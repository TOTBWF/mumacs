;;; core/transient ---  -*- lexical-binding: t; -*-

;;; Commentary:
;; `disproject' expects a slightly newer version of `transient' than Emacs 30.1 ships with.

;;; Code:

(elpaca transient)

(provide 'core/transient)
;;; transient.el ends here
